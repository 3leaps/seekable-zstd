# Release Notes

Latest releases (newest to oldest). This file keeps only the latest three.

## v0.1.2

- Add Go encoder support for writing `.szst` archives (C FFI + Go wrapper + tests).
- Improve Go binding docs and release checklist for CI artifact validation.

## v0.1.1

- Add tag-triggered GitHub Release workflow for Go prebuilt bundles.
- Add manual minisign signing flow and quality-gate coverage improvements.

## v0.1.0

- Initial alpha release: Rust core + Go/Python/Node bindings with shared fixtures.
